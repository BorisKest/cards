import 'package:flutter/material.dart';

class HomeScreanWidget extends StatelessWidget {
  const HomeScreanWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
